substitutions:
  _REGION: asia-south1
  _REPOSITORY: shoposphere

steps:
  - name: gcr.io/cloud-builders/docker
    id: build-auth
    args:
      - build
      - -f
      - apps/auth-service/Dockerfile
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/auth-service:$SHORT_SHA
      - .

  - name: gcr.io/cloud-builders/docker
    id: build-tenant
    args:
      - build
      - -f
      - apps/tenant-service/Dockerfile
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/tenant-service:$SHORT_SHA
      - .

  - name: gcr.io/cloud-builders/docker
    id: build-core
    args:
      - build
      - -f
      - apps/core-api/Dockerfile
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/core-api:$SHORT_SHA
      - .

  - name: gcr.io/cloud-builders/docker
    id: build-orders
    args:
      - build
      - -f
      - apps/order-service/Dockerfile
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/order-service:$SHORT_SHA
      - .

  - name: gcr.io/cloud-builders/docker
    id: build-reports
    args:
      - build
      - -f
      - apps/report-service/Dockerfile
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/report-service:$SHORT_SHA
      - .

  - name: gcr.io/cloud-builders/docker
    id: build-frontend
    args:
      - build
      - -f
      - frontend/Dockerfile
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/frontend:$SHORT_SHA
      - .

images:
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/auth-service:$SHORT_SHA
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/tenant-service:$SHORT_SHA
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/core-api:$SHORT_SHA
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/order-service:$SHORT_SHA
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/report-service:$SHORT_SHA
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/frontend:$SHORT_SHA
